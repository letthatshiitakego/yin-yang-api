const shortenUrl = async (originalUrl) => {
  return originalUrl;
};

/**
 * redirect url
 */
const fetchUrl = async (shortened) => {
  return shortened;
};

module.exports = {
  shortenUrl,
  fetchUrl,
};
